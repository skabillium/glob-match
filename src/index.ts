export function check(pattern: string, ...strs: string[]) {
    console.log('TODO: This will check if a string or list of strings match a glob pattern');
}

export function toRegex(pattern: string) {
    console.log("TODO: This will return a new Regex based on a glob pattern provided");
}

